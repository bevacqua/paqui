#!/usr/bin/env node

'use strict';

require('../src').configure(function (program) {
    program
        .option('-p, --prefix [file]', 'Working directory', process.cwd())
        .option('--rc [file]', '.paquirc file', '.paquirc');
}).use(module);
