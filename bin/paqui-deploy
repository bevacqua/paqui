#!/usr/bin/env node

'use strict';

var parse = require('../src/command/lib/parse.js');

require('../src').configure(function (program) {
    program
        .option('-p, --prefix [file]', 'Working directory to use')
        .option('--rc [file]', '.paquirc file', '.paquirc');
}, parse.options).use(module);
