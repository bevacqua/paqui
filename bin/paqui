#!/usr/bin/env node

'use strict';

var program = require('commander');
var pkg = require('../package.json');

program
    .version(pkg.version)
    .command('init', 'Init a component directory structure')
    .command('deploy', 'Deploy a component')
    .parse(process.argv);

