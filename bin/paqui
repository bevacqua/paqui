#!/usr/bin/env node

'use strict';

var program = require('commander');
var pkg = require('../package.json');

program
    .version(pkg.version)
    .command('init', 'Init a component directory structure')
    .command('build', 'Build a component')
    .command('deploy', 'Deploy the component')
    .parse(process.argv);

